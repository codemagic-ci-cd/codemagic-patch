// Keycap literal (`<kbd>`), shared by the command palette's footer hints and
// the sidebar search trigger. `font-sans` is explicit because the UA renders
// <kbd> in a monospace family and these caps sit inline with body copy.
export const KBD =
  "inline-flex min-w-[19px] items-center justify-center rounded-xs border border-border-strong bg-surface px-[5px] py-px font-sans text-[11px] font-semibold leading-[17px] text-fg-3";
